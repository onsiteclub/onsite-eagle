name: Build Android (EAS)

# Manual-only workflow serving four Expo apps via an `app` dropdown.
# Per BRANCH_CONVENTIONS.md §2c, this means two agents firing it at
# the same time contend for the same EXPO_TOKEN. Long-term, split into
# one workflow per app (build-<app>-android.yml) so each agent owns
# its own pipeline end-to-end.
on:
  workflow_dispatch:
    inputs:
      app:
        description: 'Expo app to build'
        required: true
        default: 'inspect'
        type: choice
        options:
          - inspect
          - timekeeper
          - operator
          - operator_2
          - field
      profile:
        description: 'Build profile'
        required: true
        default: 'preview'
        type: choice
        options:
          - development
          - preview
          - production

jobs:
  build:
    name: EAS Build — ${{ inputs.app }} (${{ inputs.profile }})
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/${{ inputs.app }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        working-directory: .

      - name: Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Build Android
        run: eas build --platform android --profile ${{ inputs.profile }} --non-interactive
