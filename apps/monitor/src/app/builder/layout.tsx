export const metadata = {
  title: 'Builder Portal — OnSite Eagle',
  description: 'View your construction project progress in real-time',
}

export default function BuilderLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
