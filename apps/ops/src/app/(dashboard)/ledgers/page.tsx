// Placeholder — Phase 6 will merge /clients + /statement into this route.
// Keeping this stub so the sidebar nav doesn't 404 during the UI port.
export default function LedgersPlaceholderPage() {
  return (
    <>
      <h1 className="page-title">Ledgers</h1>
      <div className="page-subtitle">Coming next · this replaces Clients + Statement</div>
      <div className="pdf-preview" style={{ marginTop: 24 }}>
        [ UNDER CONSTRUCTION — Phase 6 of the UI port ]
      </div>
    </>
  )
}
